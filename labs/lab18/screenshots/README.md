# Lab 18 screenshot names

Place screenshots from local execution in this directory using these names:

1. `01-nix-version.png` - `nix --version` and `nix run nixpkgs#hello`
2. `02-nix-app-running.png` - Nix-built app open in browser at `http://localhost:5000`
3. `03-nix-store-paths.png` - terminal with identical store paths from repeated `nix-build`
4. `04-pip-vs-nix-comparison.png` - terminal showing pip freeze comparison / analysis evidence
5. `05-docker-containers-running.png` - both Lab 2 Docker image and Nix Docker image responding on different ports
6. `06-docker-hash-comparison.png` - terminal with `sha256sum result` repeated and Docker save hash comparison
7. `07-docker-history.png` - terminal with `docker history` output for both images
8. `08-flake-build.png` - optional bonus: `nix flake update` / `nix build` / `nix develop`
